_reference-old/

# Node / Vercel
node_modules/
.vercel/

# Local env — never commit secrets
.env
.env.local
.env.*.local

# Legacy PHP backend secrets & uploaded CVs (kept only under _reference-old)
config.php
/uploads/cvs/*
!/uploads/cvs/.htaccess
